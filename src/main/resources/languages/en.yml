command:
  compact:
    empty: There are no items to compact.
    only-player: Only players can run this command.
    success: Compacted all available items.
  config:
    invalid-item-id: 'Unknown item ID: {0}'
    invalid-tag-id: 'Unknown item tag ID: {0}'
    no-permission: You do not have permission to view or edit configuration values.
    set: Changed {0} to {1} and saved the configuration.
    unset: unset
    view: '{0}: {1}'
  ignore:
    empty-hand: Hold an item in your main hand.
    only-player: Only players can run this command.
    set: Marked the main-hand item as a max stack-size exception.
    status: 'ignored: {0}, managed by CompactResources: {1}'
    unset: Removed the max stack-size exception from the main-hand item.
  reload:
    success: CompactResources configuration has been reloaded.
log:
  config:
    blank-regex-pattern: Skipping a regex rule with an empty pattern.
    invalid-regex-pattern: 'Skipping an invalid regex rule: {0}'
    invalid-resource-pack-uuid: 'Resetting an invalid resource pack UUID to the default: {0}'
    unknown-item-id: 'Unknown item ID is kept in the configuration: {0}'
  resource-pack:
    invalid-config: 'Skipping resource pack delivery because the configuration is invalid: {0}'
  stack-size:
    invalid-tag-id: 'Skipping an invalid item tag ID: {0}'
resource-pack:
  prompt: CompactResources applies a resource pack for compressed resource models.
